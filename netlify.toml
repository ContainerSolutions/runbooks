# Settings in the [build] context are global and are applied to all contexts
# unless otherwise overridden by more specific contexts.
[build]
  # Directory (relative to root of your repo) that contains the deploy-ready
  # HTML files and assets generated by the build. If a base directory has
  # been specified, include it in the publish directory path.
  publish = "public"

  # Default build command.
  command = "HUGO_BASEURL=$URL hugo --gc --minify"

[build.environment]
  HUGO_VERSION = "0.71.0"
  HUGO_ENABLEGITINFO = "true"

# Production context: all deploys from the Production branch set in your site's
# deploy contexts will inherit these settings.
[context.production]
  command = "HUGO_BASEURL=$URL hugo"
[context.production.environment]
  HUGO_ENV = "production"

# Deploy Preview context: all deploys generated from a pull/merge request will
# inherit these settings.
[context.deploy-preview]
  # We specify "build future" so that any content not yet published will still show up in the preview
  command = "hugo --gc --minify --buildFuture -b $DEPLOY_PRIME_URL"
  # command = "HUGO_BASEURL=$DEPLOY_PRIME_URL hugo"

# Branch Deploy context: all deploys that are not from a pull/merge request or
# from the Production branch will inherit these settings.
# [context.branch-deploy]
#   command = "HUGO_BASEURL=$DEPLOY_PRIME_URL hugo"
#   command = "hugo --gc --minify -b $DEPLOY_PRIME_URL"
# [context.branch-deploy.environment]
#   NODE_ENV = "development"
